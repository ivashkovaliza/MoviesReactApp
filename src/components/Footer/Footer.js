import React from 'react';
import './Footer.scss';
import Logo from '../Logo/Logo';

const Footer = () => {
  return (
    <footer className={'footer'}>
      <Logo />
    </footer>
  );
};

export default Footer;
