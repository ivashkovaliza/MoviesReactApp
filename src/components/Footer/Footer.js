import React from 'react';
import Logo from '../Logo/Logo';
import './Footer.scss';

const Footer = () => {
  return (
    <footer className={'footer'}>
      <Logo />
    </footer>
  );
};

export default Footer;
